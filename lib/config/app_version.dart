/// User-facing app version shown in Profile and similar screens.
/// Play Store / App Store build numbers still come from pubspec `+N`.
abstract final class AppVersion {
  static const String display = '1.0.0';
}
